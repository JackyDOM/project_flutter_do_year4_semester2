import 'package:flutter/material.dart';

class SeaDetail extends StatelessWidget {

  final Map<String, dynamic> seaDetail;
  const SeaDetail({Key? key, required this.seaDetail}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(

    );
  }
}
